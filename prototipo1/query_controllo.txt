SELECT *  
from prova 
HAVING (((prova.INDIRIZZOPECSCUOLA) Like "non disponibile"))

MANCANO CIRCA 41K

SELECT *  
from prova 
HAVING (((prova.AREAGEOGRAFICA) Like "non disponibile"))

CI SONO TUTTI


SELECT *  
from prova 
HAVING (((prova.SITOWEBSCUOLA) Like "non disponibile"))

MANCANO CIRCA 8500 SITI


SELECT *  
from prova 
HAVING (((prova.`CODICEISTITUTORIFERIMENTO`) Like "non disponibile"))

SELECT *  
from prova 
HAVING (((prova.`CODICEISTITUTORIFERIMENTO`) Like "NO"))

SELECT *  
from prova 
HAVING (((prova.`CODICEISTITUTORIFERIMENTO`) Like "NON DISPONIBILE"))

I CODICI DI ISTITUTO CI SONO TUTTI

SELECT *  
from prova 
HAVING (((prova.`PROVINCIA`) Like "non disponibile"))

CI SONO TUTTE

SELECT *  
from prova 
HAVING (((prova.`REGIONE`) Like "non disponibile"))

CI SONO TUTTE

SELECT *  
from prova 
 HAVING (((prova.`DENOMINAZIONEISTITUTORIFERIMENTO`) Like "NON DISPONIBILE"))
CI SONO TUTTI

SELECT * 
FROM `prova`
WHERE `DENOMINAZIONESCUOLA` LIKE 'NON DISPONIBILE'

NE MANCANO 30


SELECT *  
FROM `prova` 
WHERE `INDIRIZZOSCUOLA` LIKE 'NON DISPONIBILE'

NE MANCANO 1145

SELECT *  
FROM `prova` 
WHERE `CAPSCUOLA` LIKE 'NON DISPONIBILE' OR '0'
CI SONO TUTTI

SELECT *  
FROM `prova` 
WHERE `CODICECOMUNESCUOLA` LIKE 'NON DISPONIBILE'
CI SONO TUTTI


SELECT *  
FROM `prova` 
WHERE `INDICAZIONESEDEOMNICOMPRENSIVO` LIKE 'NON DISPONIBILE'

NE MANCANO 50959

DESCRIZIONE : 

PERCORS ANN.A SPEC.P NORMALE CONVITT - DI MONT - C/O IST SLOVENO ANNESSO CPIA


SELECT *  
FROM `prova`
 WHERE `DESCRIZIONECARATTERISTICASCUOLA` LIKE 'PERCORS'
1461

SELECT *  
FROM `prova`
 WHERE `DESCRIZIONECARATTERISTICASCUOLA` LIKE 'NORMALE'
48041

SELECT *  
FROM `prova`
 WHERE `DESCRIZIONECARATTERISTICASCUOLA` LIKE 'COVITT'
130

SELECT *  
FROM `prova`
 WHERE `DESCRIZIONECARATTERISTICASCUOLA` LIKE 'NON DISPONIBILE'
0



SELECT *  
FROM `prova`
 WHERE `DESCRIZIONECARATTERISTICASCUOLA` LIKE 'DI MONT'
162

SELECT *  
FROM `prova`
 WHERE `DESCRIZIONECARATTERISTICASCUOLA` LIKE 'C/O IST'
275

SELECT *  
FROM `prova`
 WHERE `DESCRIZIONECARATTERISTICASCUOLA` LIKE 'SLOVENO'
88

SELECT *  
FROM `prova`
 WHERE `DESCRIZIONECARATTERISTICASCUOLA` LIKE 'ANNESSO'
224

SELECT *  
FROM `prova`
 WHERE `DESCRIZIONECARATTERISTICASCUOLA` LIKE 'CPIA'
130


SELECT *  
FROM `prova` 
WHERE `INDICAZIONESEDEDIRETTIVO` LIKE 'no' 
43003

SELECT *  
FROM `prova` 
WHERE `INDICAZIONESEDEDIRETTIVO` LIKE 'si' 
8187


SELECT *  
FROM `prova` 
WHERE `INDICAZIONESEDEOMNICOMPRENSIVO` LIKE 'non disponibile' 
50959

SELECT *  
FROM `prova`
WHERE `INDIRIZZOEMAILSCUOLA`LIKE 'non disponibile' 
829

SELECT * 
FROM `prova` 
WHERE `SITOWEBSCUOLA` LIKE 'non disponibile' 
8079


SELECT *  
FROM `prova`
WHERE `SEDESCOLASTICA` LIKE 'no' 
51990